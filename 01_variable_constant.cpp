/*
 * Variables and Constants
 */

#include <iostream>

/*
 * MACRO to define CONSTANT value
 *
 * Name of constants should be all capital LETTERS.
 */
#define PI 3.14     // every "PI" in this source code will by "3.14"

int main()
{
    // Variable is named memory location to store data
    // Here "a" is a variable and 10 is "literal constant" and that value is
    // stored inside "a" (at memory location of a).
    int a = 10;

    /*
     * Variable Naming Rules
     *
     * 1. Name can have only "alphabates", "numbers" and "_".
     * 2. Name cannot start with number.
     * 3. Name cannot have special-symbol.
     */


    /*
     * const keyword
     *
     * 1. It is used to declare any variable as immutable (value cannot be changed)
     * 2. Must be initialized at declaration
     */

    const temp = 10;

    return 0;
}
