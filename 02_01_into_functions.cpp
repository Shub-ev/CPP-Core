#include <iostream>

/*
 * Functions
 * 1. Functions make our code modular.
 * 2. Debugging is easy.
 * 3. Code base is less error prone.
 * 4. Increases readability.
 *
 * Functions defined by yourself are called "user-defined functions".
 *
 * Function initiating the function call is called "Caller".
 * Function being called is the "Callee".
 * Function call is also called as "invocation".
 * Function must be defined before it can be called.
 *
 * Function whose definition in placed inside another function is called
 * "nested function". Nested functions are not supported in C++.
 *
 * Syntax for functions:
 * returntype function_name()       // This is "function header".
 * {
 *                                  // This is function body.
 * }
 *
 * To call a function we use the function's name followed by
 * parentheses. e.x. function_name();
 */

int main()
{
    return 0;
}
